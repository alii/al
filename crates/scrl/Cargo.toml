[package]
name = "al"
version = "0.1.0"
edition = "2024"
build = "build.rs"

[features]
# Interpreter dispatch instrumentation — forwarded to `al_vm`, where the
# dispatch loop lives. See its manifest.
op-histogram = ["al_vm/op-histogram"]

[dependencies]
al_core = { path = "../al_core" }
al_vm = { path = "../al_vm" }
clap = { version = "4", features = ["derive"] }
clap_mangen = "0.3"
percent-encoding = "2"
rustyline = "14"
serde_json = "1"
ureq = "2"

[build-dependencies]
al_core = { path = "../al_core" }

[dev-dependencies]
al_core = { path = "../al_core" }
al_vm = { path = "../al_vm", features = ["alloc-counter"] }
cranelift-codegen = { workspace = true }
cranelift-frontend = { workspace = true }
cranelift-jit = { workspace = true }
cranelift-module = { workspace = true }
cranelift-native = { workspace = true }
serde_json = "1"

[[bin]]
name = "al"
path = "src/main.rs"

