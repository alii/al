/**
 * Small arithmetic helpers.
 *
 * A doc block starting on line 0 documents the module itself — hover the `b`
 * in `b.add(..)` over in `a.al` to read this. Below line 0 the same syntax
 * documents whatever declaration follows.
 */

/**
 * Sum of `a` and `b`.
 */
pub fn add(a, b) {
	a + b
}
