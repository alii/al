{
  "name": "scarlet-lang",
  "displayName": "Scarlet Language Support",
  "description": "Scarlet programming language support for Visual Studio Code",
  "version": "0.0.1",
  "publisher": "alistaiir",
  "engines": {
    "vscode": "^1.107.0"
  },
  "categories": [
    "Programming Languages",
    "Formatters"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "languages": [
      {
        "id": "scarlet",
        "aliases": [
          "Scarlet",
          "scrl"
        ],
        "extensions": [
          ".scrl"
        ],
        "configuration": "./language-configuration.json"
      }
    ],
    "grammars": [
      {
        "language": "scarlet",
        "scopeName": "source.scrl",
        "path": "./syntaxes/scarlet.tmLanguage.json"
      }
    ],
    "configuration": {
      "title": "Scarlet",
      "properties": {
        "scarlet.binaryPath": {
          "type": "string",
          "default": "scarlet",
          "description": "Path to the Scarlet binary"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "bun run build",
    "build": "tsc --noEmit -p ./ && bun build src/extension.ts --outfile=out/extension.js --target=node --format=cjs --external vscode",
    "watch": "tsc -watch -p ./",
    "package": "vsce package --no-dependencies --allow-missing-repository --skip-license"
  },
  "dependencies": {
    "vscode-languageclient": "^9.0.1"
  },
  "devDependencies": {
    "@socketsecurity/bun-security-scanner": "^1.1.2",
    "@types/node": "^25.0.3",
    "@types/vscode": "~1.107.0",
    "@vscode/vsce": "^3.9.1",
    "typescript": "^5.9.3"
  }
}
